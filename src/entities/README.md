# Entities Layer

Business entities that the project works with (e.g., User, Post, Comment).

Each entity should have:

- `ui/` - visual representation components
- `model/` - data models, types, stores
- `api/` - entity-specific API calls (optional)

Dependency: Can import from Shared only
